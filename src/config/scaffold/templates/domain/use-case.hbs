import { UseCase } from "#contracts/UseCase"
import { injectable } from "tsyringe";
import { {{name}}Request} from "#domain/{{domainfile}}/dto/{{namefile}}.request.ts";
import { {{name}}Response} from "#domain/{{domainfile}}/dto/{{namefile}}.response.ts";

@injectable()
export class {{name}} implements UseCase< {{name}}Request, {{name}}Response> {
    constructor() {}
    async execute(request: {{name}}Request): Promise< {{name}}Response | undefined> {
        return;
    }
}